
interface BasicEntity  {
    id: ID! 
}

type Notif implements BasicEntity  {
    id: ID!
    ownsMission: Boolean!
    missionId: String
    ownerId: String! @glSkipOnClient
}

type Query {
    getNotif: Notif!
}