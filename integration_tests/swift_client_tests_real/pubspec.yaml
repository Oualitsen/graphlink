name: graphlink_swift_client_integration_tests_real
description: Swift client integration tests (real server) for GraphLink
version: 1.0.0
publish_to: none

environment:
  sdk: '>=2.19.0 <4.0.0'

dependencies:
  graphlink:
    path: ../..
