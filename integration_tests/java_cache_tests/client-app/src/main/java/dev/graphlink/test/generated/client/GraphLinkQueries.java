// GENERATED CODE - DO NOT MODIFY BY HAND. ANY MODIFICATION WILL BE LOST ON NEXT GENERATION
// Generated by GraphLink
// GitHub: https://github.com/Oualitsen/graphlink
// Site: https://graphlink.dev
// Pub.dev https://pub.dev/packages/graphlink
// ignore_for_file: use_rethrow_when_possible camel_case_types, constant_identifier_names, unused_import, non_constant_identifier_names

package dev.graphlink.test.generated.client;
import dev.graphlink.test.generated.types.GraphLinkPayload;
import dev.graphlink.test.generated.types.GetCarAndOwnerResponse;
import dev.graphlink.test.generated.types.GetOwnerResponse;
import dev.graphlink.test.generated.types.GetCarResponse;
import dev.graphlink.test.generated.types.GetCarStaleResponse;
import dev.graphlink.test.generated.types.GetCarExpiringResponse;
import dev.graphlink.test.generated.types.GetCarsCountResponse;
import dev.graphlink.test.generated.types.GetCarNameResponse;
import java.util.Map;
import java.util.HashMap;
import java.util.List;
import java.util.Arrays;
import java.util.stream.Collectors;
import java.util.Set;
import java.util.HashSet;
import java.util.ArrayList;
import dev.graphlink.test.generated.interfaces.GraphLinkJsonEncoder;
import dev.graphlink.test.generated.interfaces.GraphLinkJsonDecoder;
import dev.graphlink.test.generated.interfaces.GraphLinkClientAdapter;

public class GraphLinkQueries extends GraphLinkResolverBase {
   private final GraphLinkClientAdapter adapter;
   public GraphLinkQueries(GraphLinkClientAdapter adapter, Map<String, String> fragmentMap, GraphLinkJsonEncoder encoder, GraphLinkJsonDecoder decoder, GraphLinkCacheStore store) {
      super(fragmentMap, store, encoder, decoder);
      this.adapter = adapter;
   }
   public GetCarAndOwnerResponse getCarAndOwner(String carId, String ownerId) {
      String operationName = "getCarAndOwner";
      Map<String, Object> variables = new HashMap<>();
      variables.put("carId", carId);
      variables.put("ownerId", ownerId);

      List<GraphLinkPartialQuery> partialQueries = new ArrayList<>();
      {
        Map<String, Object> pqVars = new HashMap<>();
        pqVars.put("carId", variables.get("carId"));
        partialQueries.add(new GraphLinkPartialQuery(
          "car:getCar(id: $carId){..._all_fields_Car}",
          pqVars,
          60,
          Arrays.asList("cars", "data"),
          "getCarAndOwner_car_getCar",
          "car",
          new HashSet<>(Arrays.asList("_all_fields_Car")),
          Arrays.asList("$carId: ID!"),
          false,
          encoder
        ));
      }
      {
        Map<String, Object> pqVars = new HashMap<>();
        pqVars.put("ownerId", variables.get("ownerId"));
        partialQueries.add(new GraphLinkPartialQuery(
          "getOwner(id: $ownerId){..._all_fields_Owner}",
          pqVars,
          60,
          Arrays.asList("persons", "data"),
          "getCarAndOwner__getOwner",
          "getOwner",
          new HashSet<>(Arrays.asList("_all_fields_Owner", "_all_fields_Car")),
          Arrays.asList("$ownerId: ID!"),
          false,
          encoder
        ));
      }
      Map<String, Object> responseMap = new HashMap<>();
      Map<String, Object> staleData = new HashMap<>();
      for (var partQuery : partialQueries) {
         if(partQuery.ttl > 0) {
            try {
               GraphLinkCacheEntry entry = getFromCache(partQuery.cacheKey, partQuery.tags, partQuery.staleIfOffline);
               if(entry != null) {
                  if(entry.stale) {
                     staleData.put(partQuery.elementKey, decoder.decode(entry.data));
                  } else {
                     responseMap.put(partQuery.elementKey, decoder.decode(entry.data));
                  }
               }
            } catch (Exception ignored) {
            }
         }
      }
      List<GraphLinkPartialQuery> remaining = new ArrayList<>();
      for (var partQuery : partialQueries) {
         if(!responseMap.containsKey(partQuery.elementKey)) {
            remaining.add(partQuery);
         }
      }
      if(remaining.isEmpty()) {
         return GetCarAndOwnerResponse.fromJson(responseMap);
      }
      GraphLinkPayload payload = buildPayload(remaining, operationName, "");
      try {
         String responseText = adapter.execute(encoder.encode(payload));
         return parseToObjectAndCache(responseText, responseMap, GetCarAndOwnerResponse::fromJson, remaining);
      } catch (Exception exception) {
         responseMap.putAll(staleData);
         long remainingCount = partialQueries.stream().filter(e -> !responseMap.containsKey(e.elementKey)).count();
         if(remainingCount > 0) {
            if (exception instanceof RuntimeException) throw (RuntimeException) exception;
            throw new RuntimeException(exception);
         }
         return GetCarAndOwnerResponse.fromJson(responseMap);
      }
   }
   public GetOwnerResponse getOwner(String id) {
      String operationName = "getOwner";
      Map<String, Object> variables = new HashMap<>();
      variables.put("id", id);

      List<GraphLinkPartialQuery> partialQueries = new ArrayList<>();
      {
        Map<String, Object> pqVars = new HashMap<>();
        pqVars.put("id", variables.get("id"));
        partialQueries.add(new GraphLinkPartialQuery(
          "getOwner(id: $id){..._all_fields_Owner}",
          pqVars,
          0,
          new ArrayList<>(),
          "getOwner__getOwner",
          "getOwner",
          new HashSet<>(Arrays.asList("_all_fields_Owner", "_all_fields_Car")),
          Arrays.asList("$id: ID!"),
          false,
          encoder
        ));
      }
      Map<String, Object> responseMap = new HashMap<>();
      Map<String, Object> staleData = new HashMap<>();
      for (var partQuery : partialQueries) {
         if(partQuery.ttl > 0) {
            try {
               GraphLinkCacheEntry entry = getFromCache(partQuery.cacheKey, partQuery.tags, partQuery.staleIfOffline);
               if(entry != null) {
                  if(entry.stale) {
                     staleData.put(partQuery.elementKey, decoder.decode(entry.data));
                  } else {
                     responseMap.put(partQuery.elementKey, decoder.decode(entry.data));
                  }
               }
            } catch (Exception ignored) {
            }
         }
      }
      List<GraphLinkPartialQuery> remaining = new ArrayList<>();
      for (var partQuery : partialQueries) {
         if(!responseMap.containsKey(partQuery.elementKey)) {
            remaining.add(partQuery);
         }
      }
      if(remaining.isEmpty()) {
         return GetOwnerResponse.fromJson(responseMap);
      }
      GraphLinkPayload payload = buildPayload(remaining, operationName, "");
      try {
         String responseText = adapter.execute(encoder.encode(payload));
         return parseToObjectAndCache(responseText, responseMap, GetOwnerResponse::fromJson, remaining);
      } catch (Exception exception) {
         responseMap.putAll(staleData);
         long remainingCount = partialQueries.stream().filter(e -> !responseMap.containsKey(e.elementKey)).count();
         if(remainingCount > 0) {
            if (exception instanceof RuntimeException) throw (RuntimeException) exception;
            throw new RuntimeException(exception);
         }
         return GetOwnerResponse.fromJson(responseMap);
      }
   }
   public GetCarResponse getCar(String id) {
      String operationName = "getCar";
      Map<String, Object> variables = new HashMap<>();
      variables.put("id", id);

      List<GraphLinkPartialQuery> partialQueries = new ArrayList<>();
      {
        Map<String, Object> pqVars = new HashMap<>();
        pqVars.put("id", variables.get("id"));
        partialQueries.add(new GraphLinkPartialQuery(
          "getCar(id: $id){..._all_fields_Car}",
          pqVars,
          60,
          Arrays.asList("cars"),
          "getCar__getCar",
          "getCar",
          new HashSet<>(Arrays.asList("_all_fields_Car")),
          Arrays.asList("$id: ID!"),
          false,
          encoder
        ));
      }
      Map<String, Object> responseMap = new HashMap<>();
      Map<String, Object> staleData = new HashMap<>();
      for (var partQuery : partialQueries) {
         if(partQuery.ttl > 0) {
            try {
               GraphLinkCacheEntry entry = getFromCache(partQuery.cacheKey, partQuery.tags, partQuery.staleIfOffline);
               if(entry != null) {
                  if(entry.stale) {
                     staleData.put(partQuery.elementKey, decoder.decode(entry.data));
                  } else {
                     responseMap.put(partQuery.elementKey, decoder.decode(entry.data));
                  }
               }
            } catch (Exception ignored) {
            }
         }
      }
      List<GraphLinkPartialQuery> remaining = new ArrayList<>();
      for (var partQuery : partialQueries) {
         if(!responseMap.containsKey(partQuery.elementKey)) {
            remaining.add(partQuery);
         }
      }
      if(remaining.isEmpty()) {
         return GetCarResponse.fromJson(responseMap);
      }
      GraphLinkPayload payload = buildPayload(remaining, operationName, "");
      try {
         String responseText = adapter.execute(encoder.encode(payload));
         return parseToObjectAndCache(responseText, responseMap, GetCarResponse::fromJson, remaining);
      } catch (Exception exception) {
         responseMap.putAll(staleData);
         long remainingCount = partialQueries.stream().filter(e -> !responseMap.containsKey(e.elementKey)).count();
         if(remainingCount > 0) {
            if (exception instanceof RuntimeException) throw (RuntimeException) exception;
            throw new RuntimeException(exception);
         }
         return GetCarResponse.fromJson(responseMap);
      }
   }
   public GetCarStaleResponse getCarStale(String id) {
      String operationName = "getCarStale";
      Map<String, Object> variables = new HashMap<>();
      variables.put("id", id);

      List<GraphLinkPartialQuery> partialQueries = new ArrayList<>();
      {
        Map<String, Object> pqVars = new HashMap<>();
        pqVars.put("id", variables.get("id"));
        partialQueries.add(new GraphLinkPartialQuery(
          "getCarStale(id: $id){..._all_fields_Car}",
          pqVars,
          1,
          Arrays.asList("cars"),
          "getCarStale__getCarStale",
          "getCarStale",
          new HashSet<>(Arrays.asList("_all_fields_Car")),
          Arrays.asList("$id: ID!"),
          true,
          encoder
        ));
      }
      Map<String, Object> responseMap = new HashMap<>();
      Map<String, Object> staleData = new HashMap<>();
      for (var partQuery : partialQueries) {
         if(partQuery.ttl > 0) {
            try {
               GraphLinkCacheEntry entry = getFromCache(partQuery.cacheKey, partQuery.tags, partQuery.staleIfOffline);
               if(entry != null) {
                  if(entry.stale) {
                     staleData.put(partQuery.elementKey, decoder.decode(entry.data));
                  } else {
                     responseMap.put(partQuery.elementKey, decoder.decode(entry.data));
                  }
               }
            } catch (Exception ignored) {
            }
         }
      }
      List<GraphLinkPartialQuery> remaining = new ArrayList<>();
      for (var partQuery : partialQueries) {
         if(!responseMap.containsKey(partQuery.elementKey)) {
            remaining.add(partQuery);
         }
      }
      if(remaining.isEmpty()) {
         return GetCarStaleResponse.fromJson(responseMap);
      }
      GraphLinkPayload payload = buildPayload(remaining, operationName, "");
      try {
         String responseText = adapter.execute(encoder.encode(payload));
         return parseToObjectAndCache(responseText, responseMap, GetCarStaleResponse::fromJson, remaining);
      } catch (Exception exception) {
         responseMap.putAll(staleData);
         long remainingCount = partialQueries.stream().filter(e -> !responseMap.containsKey(e.elementKey)).count();
         if(remainingCount > 0) {
            if (exception instanceof RuntimeException) throw (RuntimeException) exception;
            throw new RuntimeException(exception);
         }
         return GetCarStaleResponse.fromJson(responseMap);
      }
   }
   public GetCarExpiringResponse getCarExpiring(String id) {
      String operationName = "getCarExpiring";
      Map<String, Object> variables = new HashMap<>();
      variables.put("id", id);

      List<GraphLinkPartialQuery> partialQueries = new ArrayList<>();
      {
        Map<String, Object> pqVars = new HashMap<>();
        pqVars.put("id", variables.get("id"));
        partialQueries.add(new GraphLinkPartialQuery(
          "getCarExpiring(id: $id){..._all_fields_Car}",
          pqVars,
          1,
          Arrays.asList("cars"),
          "getCarExpiring__getCarExpiring",
          "getCarExpiring",
          new HashSet<>(Arrays.asList("_all_fields_Car")),
          Arrays.asList("$id: ID!"),
          false,
          encoder
        ));
      }
      Map<String, Object> responseMap = new HashMap<>();
      Map<String, Object> staleData = new HashMap<>();
      for (var partQuery : partialQueries) {
         if(partQuery.ttl > 0) {
            try {
               GraphLinkCacheEntry entry = getFromCache(partQuery.cacheKey, partQuery.tags, partQuery.staleIfOffline);
               if(entry != null) {
                  if(entry.stale) {
                     staleData.put(partQuery.elementKey, decoder.decode(entry.data));
                  } else {
                     responseMap.put(partQuery.elementKey, decoder.decode(entry.data));
                  }
               }
            } catch (Exception ignored) {
            }
         }
      }
      List<GraphLinkPartialQuery> remaining = new ArrayList<>();
      for (var partQuery : partialQueries) {
         if(!responseMap.containsKey(partQuery.elementKey)) {
            remaining.add(partQuery);
         }
      }
      if(remaining.isEmpty()) {
         return GetCarExpiringResponse.fromJson(responseMap);
      }
      GraphLinkPayload payload = buildPayload(remaining, operationName, "");
      try {
         String responseText = adapter.execute(encoder.encode(payload));
         return parseToObjectAndCache(responseText, responseMap, GetCarExpiringResponse::fromJson, remaining);
      } catch (Exception exception) {
         responseMap.putAll(staleData);
         long remainingCount = partialQueries.stream().filter(e -> !responseMap.containsKey(e.elementKey)).count();
         if(remainingCount > 0) {
            if (exception instanceof RuntimeException) throw (RuntimeException) exception;
            throw new RuntimeException(exception);
         }
         return GetCarExpiringResponse.fromJson(responseMap);
      }
   }
   public GetCarsCountResponse getCarsCount() {
      String operationName = "getCarsCount";
      Map<String, Object> variables = new HashMap<>();

      List<GraphLinkPartialQuery> partialQueries = new ArrayList<>();
      {
        Map<String, Object> pqVars = new HashMap<>();
        partialQueries.add(new GraphLinkPartialQuery(
          "getCarsCount",
          pqVars,
          60,
          new ArrayList<>(),
          "getCarsCount__getCarsCount",
          "getCarsCount",
          new HashSet<>(),
          new ArrayList<>(),
          false,
          encoder
        ));
      }
      Map<String, Object> responseMap = new HashMap<>();
      Map<String, Object> staleData = new HashMap<>();
      for (var partQuery : partialQueries) {
         if(partQuery.ttl > 0) {
            try {
               GraphLinkCacheEntry entry = getFromCache(partQuery.cacheKey, partQuery.tags, partQuery.staleIfOffline);
               if(entry != null) {
                  if(entry.stale) {
                     staleData.put(partQuery.elementKey, decoder.decode(entry.data));
                  } else {
                     responseMap.put(partQuery.elementKey, decoder.decode(entry.data));
                  }
               }
            } catch (Exception ignored) {
            }
         }
      }
      List<GraphLinkPartialQuery> remaining = new ArrayList<>();
      for (var partQuery : partialQueries) {
         if(!responseMap.containsKey(partQuery.elementKey)) {
            remaining.add(partQuery);
         }
      }
      if(remaining.isEmpty()) {
         return GetCarsCountResponse.fromJson(responseMap);
      }
      GraphLinkPayload payload = buildPayload(remaining, operationName, "");
      try {
         String responseText = adapter.execute(encoder.encode(payload));
         return parseToObjectAndCache(responseText, responseMap, GetCarsCountResponse::fromJson, remaining);
      } catch (Exception exception) {
         responseMap.putAll(staleData);
         long remainingCount = partialQueries.stream().filter(e -> !responseMap.containsKey(e.elementKey)).count();
         if(remainingCount > 0) {
            if (exception instanceof RuntimeException) throw (RuntimeException) exception;
            throw new RuntimeException(exception);
         }
         return GetCarsCountResponse.fromJson(responseMap);
      }
   }
   public GetCarNameResponse getCarName(String id) {
      String operationName = "getCarName";
      Map<String, Object> variables = new HashMap<>();
      variables.put("id", id);

      List<GraphLinkPartialQuery> partialQueries = new ArrayList<>();
      {
        Map<String, Object> pqVars = new HashMap<>();
        pqVars.put("id", variables.get("id"));
        partialQueries.add(new GraphLinkPartialQuery(
          "getCarName(id: $id)",
          pqVars,
          0,
          new ArrayList<>(),
          "getCarName__getCarName",
          "getCarName",
          new HashSet<>(),
          Arrays.asList("$id: ID!"),
          false,
          encoder
        ));
      }
      Map<String, Object> responseMap = new HashMap<>();
      Map<String, Object> staleData = new HashMap<>();
      for (var partQuery : partialQueries) {
         if(partQuery.ttl > 0) {
            try {
               GraphLinkCacheEntry entry = getFromCache(partQuery.cacheKey, partQuery.tags, partQuery.staleIfOffline);
               if(entry != null) {
                  if(entry.stale) {
                     staleData.put(partQuery.elementKey, decoder.decode(entry.data));
                  } else {
                     responseMap.put(partQuery.elementKey, decoder.decode(entry.data));
                  }
               }
            } catch (Exception ignored) {
            }
         }
      }
      List<GraphLinkPartialQuery> remaining = new ArrayList<>();
      for (var partQuery : partialQueries) {
         if(!responseMap.containsKey(partQuery.elementKey)) {
            remaining.add(partQuery);
         }
      }
      if(remaining.isEmpty()) {
         return GetCarNameResponse.fromJson(responseMap);
      }
      GraphLinkPayload payload = buildPayload(remaining, operationName, "");
      try {
         String responseText = adapter.execute(encoder.encode(payload));
         return parseToObjectAndCache(responseText, responseMap, GetCarNameResponse::fromJson, remaining);
      } catch (Exception exception) {
         responseMap.putAll(staleData);
         long remainingCount = partialQueries.stream().filter(e -> !responseMap.containsKey(e.elementKey)).count();
         if(remainingCount > 0) {
            if (exception instanceof RuntimeException) throw (RuntimeException) exception;
            throw new RuntimeException(exception);
         }
         return GetCarNameResponse.fromJson(responseMap);
      }
   }
   private GraphLinkPayload buildPayload(List<GraphLinkPartialQuery> partQueries, String operationName, String directives) {
      Map<String, Object> variables = new HashMap<>();
      for (var partQuery : partQueries) {
         variables.putAll(partQuery.variables);
      }
      StringBuilder queryBuilder = new StringBuilder("query " + operationName);
      Set<String> args = new HashSet<>();
      for (var partQuery : partQueries) {
         args.addAll(partQuery.argumentDeclarations);
      }
      if(!args.isEmpty()) {
         queryBuilder.append("(");
         queryBuilder.append(String.join(", ", args));
         queryBuilder.append(")");
      }
      if(!directives.isEmpty()) {
         queryBuilder.append(directives);
      }
      queryBuilder.append("{");
      for (var partQuery : partQueries) {
         queryBuilder.append(partQuery.query);
      }
      queryBuilder.append("}");
      Set<String> fragmentNames = new HashSet<>();
      for (var partQuery : partQueries) {
         fragmentNames.addAll(partQuery.fragmentNames);
      }
      StringBuilder fragmentsBuilder = new StringBuilder();
      for (var fragName : fragmentNames) {
         fragmentsBuilder.append(fragmentMap.get(fragName));
      }
      queryBuilder.append(fragmentsBuilder);
      return GraphLinkPayload.builder().query(queryBuilder.toString()).operationName(operationName).variables(variables).build();
   }
}

