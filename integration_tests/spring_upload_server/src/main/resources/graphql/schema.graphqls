# Upload scalar is registered automatically by multipart-spring-graphql.
# Do NOT redeclare it here — it would cause a duplicate definition error at startup.

type Query {
  ping: Boolean!
}

type Mutation {
  uploadOneFile(userId: ID!, file: Upload!): Boolean!
  uploadFileList(userId: ID!, files: [Upload!]!): Boolean!
}
