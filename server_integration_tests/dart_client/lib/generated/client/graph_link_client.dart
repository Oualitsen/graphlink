// GENERATED CODE - DO NOT MODIFY BY HAND. ANY MODIFICATION WILL BE LOST ON NEXT GENERATION
// Generated by GraphLink dev
// GitHub: https://github.com/Oualitsen/graphlink
// Site: https://graphlink.dev
// Pub.dev https://pub.dev/packages/graphlink
// ignore_for_file: use_rethrow_when_possible, camel_case_types, constant_identifier_names, unused_import, non_constant_identifier_names, no_leading_underscores_for_local_identifiers, unused_local_variable, annotate_overrides, library_private_types_in_public_api

import 'dart:convert';
import 'dart:async';
import 'dart:math';
import 'graph_link_dio_adapter.dart';
import 'graph_link_websocket_adapter.dart';
import 'package:graphlink_server_integration_tests_dart_client/generated/types/graph_link_payload.dart';
import 'package:graphlink_server_integration_tests_dart_client/generated/types/graph_link_error.dart';
import 'package:graphlink_server_integration_tests_dart_client/generated/types/graph_link_subscription_payload.dart';
import 'package:graphlink_server_integration_tests_dart_client/generated/enums/graph_link_ack_status.dart';
import 'package:graphlink_server_integration_tests_dart_client/generated/interfaces/graph_link_subscription_error_message_base.dart';
import 'package:graphlink_server_integration_tests_dart_client/generated/types/graph_link_subscription_error_message.dart';
import 'package:graphlink_server_integration_tests_dart_client/generated/types/graph_link_subscription_message.dart';
import 'package:graphlink_server_integration_tests_dart_client/generated/interfaces/graph_link_full_response.dart';
import 'package:graphlink_server_integration_tests_dart_client/generated/types/get_author_response.dart';
import 'package:graphlink_server_integration_tests_dart_client/generated/types/get_article_response.dart';
import 'package:graphlink_server_integration_tests_dart_client/generated/types/list_authors_response.dart';
import 'package:graphlink_server_integration_tests_dart_client/generated/types/list_articles_response.dart';
import 'package:graphlink_server_integration_tests_dart_client/generated/types/create_article_response.dart';
import 'package:graphlink_server_integration_tests_dart_client/generated/types/update_article_response.dart';
import 'package:graphlink_server_integration_tests_dart_client/generated/types/delete_article_response.dart';
import 'package:graphlink_server_integration_tests_dart_client/generated/types/article_created_response.dart';
import 'package:graphlink_server_integration_tests_dart_client/generated/types/article_updated_response.dart';
import 'package:graphlink_server_integration_tests_dart_client/generated/types/get_author_full_response.dart';
import 'package:graphlink_server_integration_tests_dart_client/generated/types/get_article_full_response.dart';
import 'package:graphlink_server_integration_tests_dart_client/generated/types/list_authors_full_response.dart';
import 'package:graphlink_server_integration_tests_dart_client/generated/types/list_articles_full_response.dart';
import 'package:graphlink_server_integration_tests_dart_client/generated/types/create_article_full_response.dart';
import 'package:graphlink_server_integration_tests_dart_client/generated/types/update_article_full_response.dart';
import 'package:graphlink_server_integration_tests_dart_client/generated/types/delete_article_full_response.dart';
import 'package:graphlink_server_integration_tests_dart_client/generated/types/article_created_full_response.dart';
import 'package:graphlink_server_integration_tests_dart_client/generated/types/article_updated_full_response.dart';
import 'package:graphlink_server_integration_tests_dart_client/generated/inputs/create_article_input.dart';
import 'package:graphlink_server_integration_tests_dart_client/generated/inputs/update_article_input.dart';

const tagKeyPrefix = '__tag__';

class _GraphLinkCacheEntry {
  final String data;
  final int expiry;
  final bool stale;

  _GraphLinkCacheEntry(this.data, this.expiry) : stale = false;

  _GraphLinkCacheEntry._(this.data, this.expiry, this.stale);

  factory _GraphLinkCacheEntry.fromJson(Map<String, dynamic> json) {
    return _GraphLinkCacheEntry(json['data'] as String, json['expiry'] as int);
  }

  Map<String, dynamic> toJson() {
    return {'data': data, 'expiry': expiry};
  }

  String encode() => jsonEncode(toJson());

  bool get isExpired => DateTime.now().millisecondsSinceEpoch > expiry;

  _GraphLinkCacheEntry asStale() =>
      _GraphLinkCacheEntry._(data, expiry, true);
}


class _Lock {
  Future<void> _last = Future.value();

  Future<T> synchronized<T>(Future<T> Function() fn) {
    final next = _last.then((_) => fn());
    _last = next.then((_) {}, onError: (_) {});
    return next;
  }
}


class _GraphLinkTagEntry {
  final Set<String> keys = {};

  _GraphLinkTagEntry(Set<String> keys) {
    this.keys.addAll(keys);
  }

  factory _GraphLinkTagEntry.fromJson(Map<String, dynamic> json) {
    return _GraphLinkTagEntry((json['keys'] as List).cast<String>().toSet());
  }

  Map<String, dynamic> toJson() {
    return {'keys': keys.toList()};
  }

  String encode() => jsonEncode(toJson());

  static _GraphLinkTagEntry decode(String raw) => _GraphLinkTagEntry.fromJson(jsonDecode(raw));

  void add(String key) => keys.add(key);

  void remove(String key) => keys.remove(key);
}


class _GraphLinkPartialQuery {
  final String query;
  final Map<String, dynamic> variables;
  final int ttl;
  final List<String> tags;
  final String operationName;
  final String elementKey;
  final Set<String> fragmentNames;
  final List<String> argumentDeclarations;
  final bool staleIfOffline;
  late final String? cacheKey;


  _GraphLinkPartialQuery({
    required this.query,
    required this.variables,
    required this.ttl,
    required this.tags,
    required this.operationName,
    required this.elementKey,
    required this.fragmentNames,
    required this.argumentDeclarations,
    required this.staleIfOffline,
  }) {
    if (ttl == 0) {
      cacheKey = null;
    } else {
      cacheKey = computeCacheKey(operationName, variables);
    }
  }

  static String computeCacheKey(String operationName, Map<String, dynamic> variables) {
    final sorted = Map.fromEntries(
      variables.entries.toList()..sort((a, b) => a.key.compareTo(b.key)),
    );
    final raw = '$operationName:${jsonEncode(sorted)}';
    return _fnv1a(raw);
  }

  static String _fnv1a(String input) {
    var hash = 0x811c9dc5;
    for (final c in input.codeUnits) {
      hash ^= c;
      hash = (hash * 0x01000193) & 0xFFFFFFFF;
    }
    return hash.toRadixString(16).padLeft(8, '0');
  }
}


abstract class GraphLinkCacheStore {
  Future<void> set(String key, String value);
  Future<String?> get(String key);
  Future<void> invalidate(String key);
  Future<void> invalidateAll();
}

class InMemoryGraphLinkCacheStore implements GraphLinkCacheStore {
  final _store = <String, String>{};

  @override
  Future<void> set(String key, String value) async {
    _store[key] = value;
  }

  @override
  Future<String?> get(String key) async {
    return _store[key];
  }

  @override
  Future<void> invalidate(String key) async {
    _store.remove(key);
  }

  @override
  Future<void> invalidateAll() async {
    _store.clear();
  }
}

class GraphLinkQueries extends _ResolverBase {
   late final Map<String, String> __gl_fragmentMap__;

   GraphLinkQueries(Future<String> Function(String payload) httpAdapter, Map<String, String> fragmentMap, GraphLinkCacheStore store, Map<String, _Lock> __gl_tagLocks__) : super(store, __gl_tagLocks__, httpAdapter) {
      __gl_fragmentMap__ = fragmentMap;
   }
   Future<GetAuthorResponse> getAuthor({
      required String id
   }) async {
      const __gl_operationName__ = 'getAuthor';
      final __gl_variables__ = <String, dynamic>{
         'id': id,
      };

      final __gl_partialQueries__ = [_GraphLinkPartialQuery(
        query: 'getAuthor(id: \$id){..._all_fields_Author}',
        operationName: "getAuthor__getAuthor",
        tags: [],
        ttl: 0,
        elementKey: 'getAuthor',
        fragmentNames: {"_all_fields_Author", "_all_fields_Article"},
        argumentDeclarations: ["\$id: ID!"],
        variables: {
      'id': __gl_variables__['id'],
      },
        staleIfOffline: false
      )
      ];
      final __gl_responseMap__ = <String, dynamic>{};
      final __gl_staleData__ = <String, dynamic>{};
      final __gl_cacheFetchFutures__ = <Future>[];
      for (var partQuery in __gl_partialQueries__.where((e) => e.ttl > 0)) {
         __gl_cacheFetchFutures__.add(_getFromCache(partQuery.cacheKey!, partQuery.tags, partQuery.staleIfOffline)
         .asStream().where((e) => e != null).map((e) => e!).first.then((entry) {
            if (entry.stale) {
               __gl_staleData__[partQuery.elementKey] = jsonDecode(entry.data);
            } else {
               __gl_responseMap__[partQuery.elementKey] = jsonDecode(entry.data);
            }
         }));
      }
      await Future.wait(__gl_cacheFetchFutures__.map((f) => f.catchError((_) => null)));
      var __gl_remaining__ = __gl_partialQueries__.where((e) => !__gl_responseMap__.containsKey(e.elementKey)).toSet();
      if (__gl_remaining__.isEmpty) {
         return GetAuthorFullResponse.fromJson({'data': __gl_responseMap__}).data!;
      }
      final __gl_remainingQueries__ = __gl_partialQueries__.where((e) => !__gl_responseMap__.containsKey(e.elementKey)).toList();
      final __gl_payload__ = _buildPayload(__gl_remainingQueries__, __gl_operationName__, '');
      try {
         final __gl_responseText__ = await _getFromSource(__gl_payload__);
         return _parseToObjectAndCache(__gl_responseText__, __gl_responseMap__, GetAuthorFullResponse.fromJson, __gl_remaining__, false).data!;
      } catch (exception) {
         __gl_responseMap__.addAll(__gl_staleData__);
         final remainingCount = __gl_partialQueries__.where((e) => !__gl_responseMap__.containsKey(e.elementKey)).length;
         if (remainingCount > 0) {
            rethrow;
         }
         return GetAuthorFullResponse.fromJson({'data': __gl_responseMap__}).data!;
      }
   }
   Future<GetArticleResponse> getArticle({
      required String id
   }) async {
      const __gl_operationName__ = 'getArticle';
      final __gl_variables__ = <String, dynamic>{
         'id': id,
      };

      final __gl_partialQueries__ = [_GraphLinkPartialQuery(
        query: 'getArticle(id: \$id){..._all_fields_Article}',
        operationName: "getArticle__getArticle",
        tags: [],
        ttl: 0,
        elementKey: 'getArticle',
        fragmentNames: {"_all_fields_Article"},
        argumentDeclarations: ["\$id: ID!"],
        variables: {
      'id': __gl_variables__['id'],
      },
        staleIfOffline: false
      )
      ];
      final __gl_responseMap__ = <String, dynamic>{};
      final __gl_staleData__ = <String, dynamic>{};
      final __gl_cacheFetchFutures__ = <Future>[];
      for (var partQuery in __gl_partialQueries__.where((e) => e.ttl > 0)) {
         __gl_cacheFetchFutures__.add(_getFromCache(partQuery.cacheKey!, partQuery.tags, partQuery.staleIfOffline)
         .asStream().where((e) => e != null).map((e) => e!).first.then((entry) {
            if (entry.stale) {
               __gl_staleData__[partQuery.elementKey] = jsonDecode(entry.data);
            } else {
               __gl_responseMap__[partQuery.elementKey] = jsonDecode(entry.data);
            }
         }));
      }
      await Future.wait(__gl_cacheFetchFutures__.map((f) => f.catchError((_) => null)));
      var __gl_remaining__ = __gl_partialQueries__.where((e) => !__gl_responseMap__.containsKey(e.elementKey)).toSet();
      if (__gl_remaining__.isEmpty) {
         return GetArticleFullResponse.fromJson({'data': __gl_responseMap__}).data!;
      }
      final __gl_remainingQueries__ = __gl_partialQueries__.where((e) => !__gl_responseMap__.containsKey(e.elementKey)).toList();
      final __gl_payload__ = _buildPayload(__gl_remainingQueries__, __gl_operationName__, '');
      try {
         final __gl_responseText__ = await _getFromSource(__gl_payload__);
         return _parseToObjectAndCache(__gl_responseText__, __gl_responseMap__, GetArticleFullResponse.fromJson, __gl_remaining__, false).data!;
      } catch (exception) {
         __gl_responseMap__.addAll(__gl_staleData__);
         final remainingCount = __gl_partialQueries__.where((e) => !__gl_responseMap__.containsKey(e.elementKey)).length;
         if (remainingCount > 0) {
            rethrow;
         }
         return GetArticleFullResponse.fromJson({'data': __gl_responseMap__}).data!;
      }
   }
   Future<ListAuthorsResponse> listAuthors() async {
      const __gl_operationName__ = 'listAuthors';
      final __gl_variables__ = <String, dynamic>{
      };

      final __gl_partialQueries__ = [_GraphLinkPartialQuery(
        query: 'listAuthors{..._all_fields_Author}',
        operationName: "listAuthors__listAuthors",
        tags: [],
        ttl: 0,
        elementKey: 'listAuthors',
        fragmentNames: {"_all_fields_Author", "_all_fields_Article"},
        argumentDeclarations: [],
        variables: {},
        staleIfOffline: false
      )
      ];
      final __gl_responseMap__ = <String, dynamic>{};
      final __gl_staleData__ = <String, dynamic>{};
      final __gl_cacheFetchFutures__ = <Future>[];
      for (var partQuery in __gl_partialQueries__.where((e) => e.ttl > 0)) {
         __gl_cacheFetchFutures__.add(_getFromCache(partQuery.cacheKey!, partQuery.tags, partQuery.staleIfOffline)
         .asStream().where((e) => e != null).map((e) => e!).first.then((entry) {
            if (entry.stale) {
               __gl_staleData__[partQuery.elementKey] = jsonDecode(entry.data);
            } else {
               __gl_responseMap__[partQuery.elementKey] = jsonDecode(entry.data);
            }
         }));
      }
      await Future.wait(__gl_cacheFetchFutures__.map((f) => f.catchError((_) => null)));
      var __gl_remaining__ = __gl_partialQueries__.where((e) => !__gl_responseMap__.containsKey(e.elementKey)).toSet();
      if (__gl_remaining__.isEmpty) {
         return ListAuthorsFullResponse.fromJson({'data': __gl_responseMap__}).data!;
      }
      final __gl_remainingQueries__ = __gl_partialQueries__.where((e) => !__gl_responseMap__.containsKey(e.elementKey)).toList();
      final __gl_payload__ = _buildPayload(__gl_remainingQueries__, __gl_operationName__, '');
      try {
         final __gl_responseText__ = await _getFromSource(__gl_payload__);
         return _parseToObjectAndCache(__gl_responseText__, __gl_responseMap__, ListAuthorsFullResponse.fromJson, __gl_remaining__, false).data!;
      } catch (exception) {
         __gl_responseMap__.addAll(__gl_staleData__);
         final remainingCount = __gl_partialQueries__.where((e) => !__gl_responseMap__.containsKey(e.elementKey)).length;
         if (remainingCount > 0) {
            rethrow;
         }
         return ListAuthorsFullResponse.fromJson({'data': __gl_responseMap__}).data!;
      }
   }
   Future<ListArticlesResponse> listArticles() async {
      const __gl_operationName__ = 'listArticles';
      final __gl_variables__ = <String, dynamic>{
      };

      final __gl_partialQueries__ = [_GraphLinkPartialQuery(
        query: 'listArticles{..._all_fields_Article}',
        operationName: "listArticles__listArticles",
        tags: [],
        ttl: 0,
        elementKey: 'listArticles',
        fragmentNames: {"_all_fields_Article"},
        argumentDeclarations: [],
        variables: {},
        staleIfOffline: false
      )
      ];
      final __gl_responseMap__ = <String, dynamic>{};
      final __gl_staleData__ = <String, dynamic>{};
      final __gl_cacheFetchFutures__ = <Future>[];
      for (var partQuery in __gl_partialQueries__.where((e) => e.ttl > 0)) {
         __gl_cacheFetchFutures__.add(_getFromCache(partQuery.cacheKey!, partQuery.tags, partQuery.staleIfOffline)
         .asStream().where((e) => e != null).map((e) => e!).first.then((entry) {
            if (entry.stale) {
               __gl_staleData__[partQuery.elementKey] = jsonDecode(entry.data);
            } else {
               __gl_responseMap__[partQuery.elementKey] = jsonDecode(entry.data);
            }
         }));
      }
      await Future.wait(__gl_cacheFetchFutures__.map((f) => f.catchError((_) => null)));
      var __gl_remaining__ = __gl_partialQueries__.where((e) => !__gl_responseMap__.containsKey(e.elementKey)).toSet();
      if (__gl_remaining__.isEmpty) {
         return ListArticlesFullResponse.fromJson({'data': __gl_responseMap__}).data!;
      }
      final __gl_remainingQueries__ = __gl_partialQueries__.where((e) => !__gl_responseMap__.containsKey(e.elementKey)).toList();
      final __gl_payload__ = _buildPayload(__gl_remainingQueries__, __gl_operationName__, '');
      try {
         final __gl_responseText__ = await _getFromSource(__gl_payload__);
         return _parseToObjectAndCache(__gl_responseText__, __gl_responseMap__, ListArticlesFullResponse.fromJson, __gl_remaining__, false).data!;
      } catch (exception) {
         __gl_responseMap__.addAll(__gl_staleData__);
         final remainingCount = __gl_partialQueries__.where((e) => !__gl_responseMap__.containsKey(e.elementKey)).length;
         if (remainingCount > 0) {
            rethrow;
         }
         return ListArticlesFullResponse.fromJson({'data': __gl_responseMap__}).data!;
      }
   }
   Future<String> _getFromSource(GraphLinkPayload payload) async {
      return await glCallAdapter(payload);
   }
   GraphLinkPayload _buildPayload(List<_GraphLinkPartialQuery> partQueries, String operationName, String directives) {
      final Map<String, dynamic> variables = {};
      for (var partQuery in partQueries) {
         variables.addAll(partQuery.variables);
      }
      final queryBuilder = StringBuffer("query ${operationName}");
      final args = partQueries.expand((e) => e.argumentDeclarations).toSet();
      if (args.isNotEmpty) {
         queryBuilder.write("(");
         queryBuilder.writeAll(args, ", ");
         queryBuilder.write(")");
      }
      if (directives.isNotEmpty) {
         queryBuilder.write(directives);
      }
      queryBuilder.write("{");
      for (var partQuery in partQueries) {
         queryBuilder.write(partQuery.query);
         queryBuilder.write(" ");
      }
      queryBuilder.write("}");
      final fragments = partQueries.expand((e) => e.fragmentNames).toSet().map((fragName) => __gl_fragmentMap__[fragName]!).join();
      queryBuilder.write(fragments);
      return GraphLinkPayload(query: queryBuilder.toString(), operationName: operationName, variables: variables);
   }
   T _parseToObjectAndCache<T extends GraphLinkFullResponse>(String data, Map<String, dynamic> cachedResponse, T Function(Map<String, dynamic> json) parser, Set<_GraphLinkPartialQuery> remainingQueries, bool captureErrors) {
      final result = jsonDecode(data) as Map<String, dynamic>;
      final rawData = result["data"];
      final dataMap = rawData as Map<String, dynamic>? ?? <String, dynamic>{};
      for (var q in remainingQueries) {
         if (q.ttl > 0 && dataMap[q.elementKey] != null) {
            final entry = _GraphLinkCacheEntry(jsonEncode(dataMap[q.elementKey]), DateTime.now().millisecondsSinceEpoch + q.ttl * 1000);
            __gl_store__.set(q.cacheKey!, jsonEncode(entry.toJson()));
            if (q.tags.isNotEmpty) {
               _addKeyToTags(q.cacheKey!, q.tags);
            }
         }
      }
      dataMap.addAll(cachedResponse);
      final fullResponse = <String, dynamic>{'data': rawData != null ? dataMap : null};
      if (result["errors"] != null) {
         fullResponse["errors"] = result["errors"];
      }
      final parsed = parser.call(fullResponse);
      if (captureErrors) {
         return parsed;
      }
      final errors = result["errors"] as List?;
      if (errors != null && errors.isNotEmpty) {
         throw parsed.errors!;
      }
      return parsed;
   }
}
class GraphLinkMutations extends _ResolverBase {

   GraphLinkMutations(Future<String> Function(String payload) httpAdapter, GraphLinkCacheStore store, Map<String, _Lock> __gl_tagLocks__) : super(store, __gl_tagLocks__, httpAdapter) {
   }
   Future<CreateArticleResponse> createArticle({
      required CreateArticleInput input
   }) async {
      const __gl_operationName__ = 'createArticle';
      const __gl_query__ = '''mutation createArticle(\$input: CreateArticleInput!){createArticle(input: \$input){..._all_fields_Article}} fragment _all_fields_Article on Article{id title authorId author{id name}}''';
      final __gl_variables__ = <String, dynamic>{
         'input': input.toJson(),
      };

      final __gl_payload__ = GraphLinkPayload(query: __gl_query__, operationName: __gl_operationName__, variables: __gl_variables__);
      final __gl_response__ = await glCallAdapter(__gl_payload__);
      final __gl_result__ = CreateArticleFullResponse.fromJson(jsonDecode(__gl_response__));
      if (__gl_result__.errors != null) {
        throw __gl_result__.errors!;
      }
      // no tag to invalidate
      return __gl_result__.data!;

   }
   Future<UpdateArticleResponse> updateArticle({
      required UpdateArticleInput input
   }) async {
      const __gl_operationName__ = 'updateArticle';
      const __gl_query__ = '''mutation updateArticle(\$input: UpdateArticleInput!){updateArticle(input: \$input){..._all_fields_Article}} fragment _all_fields_Article on Article{id title authorId author{id name}}''';
      final __gl_variables__ = <String, dynamic>{
         'input': input.toJson(),
      };

      final __gl_payload__ = GraphLinkPayload(query: __gl_query__, operationName: __gl_operationName__, variables: __gl_variables__);
      final __gl_response__ = await glCallAdapter(__gl_payload__);
      final __gl_result__ = UpdateArticleFullResponse.fromJson(jsonDecode(__gl_response__));
      if (__gl_result__.errors != null) {
        throw __gl_result__.errors!;
      }
      // no tag to invalidate
      return __gl_result__.data!;

   }
   Future<DeleteArticleResponse> deleteArticle({
      required String id
   }) async {
      const __gl_operationName__ = 'deleteArticle';
      const __gl_query__ = '''mutation deleteArticle(\$id: ID!){deleteArticle(id: \$id)}''';
      final __gl_variables__ = <String, dynamic>{
         'id': id,
      };

      final __gl_payload__ = GraphLinkPayload(query: __gl_query__, operationName: __gl_operationName__, variables: __gl_variables__);
      final __gl_response__ = await glCallAdapter(__gl_payload__);
      final __gl_result__ = DeleteArticleFullResponse.fromJson(jsonDecode(__gl_response__));
      if (__gl_result__.errors != null) {
        throw __gl_result__.errors!;
      }
      // no tag to invalidate
      return __gl_result__.data!;

   }
}
class GraphLinkSubscriptions extends _ResolverBase {
   late final _SubscriptionHandler __gl_handler__;
   GraphLinkSubscriptions(Future<String> Function(String payload) httpAdapter, GraphLinkWebSocketAdapter adapter, GraphLinkCacheStore store, Map<String, _Lock> __gl_tagLocks__) : super(store, __gl_tagLocks__, httpAdapter) {
      __gl_handler__ = _SubscriptionHandler(adapter);
   }
   Stream<ArticleCreatedResponse> articleCreated() {
      const __gl_operationName__ = 'articleCreated';
      const __gl_query__ = '''subscription articleCreated{articleCreated{..._all_fields_Article}} fragment _all_fields_Article on Article{id title authorId author{id name}}''';
      final __gl_variables__ = <String, dynamic>{
      };

      final __gl_payload__ = GraphLinkPayload(query: __gl_query__, operationName: __gl_operationName__, variables: __gl_variables__);
         return __gl_handler__.handle(__gl_payload__)
         .map((e) {
           return ArticleCreatedResponse.fromJson(e);
         });
   }
   Stream<ArticleUpdatedResponse> articleUpdated({
      required String id
   }) {
      const __gl_operationName__ = 'articleUpdated';
      const __gl_query__ = '''subscription articleUpdated(\$id: ID!){articleUpdated(id: \$id){..._all_fields_Article}} fragment _all_fields_Article on Article{id title authorId author{id name}}''';
      final __gl_variables__ = <String, dynamic>{
         'id': id,
      };

      final __gl_payload__ = GraphLinkPayload(query: __gl_query__, operationName: __gl_operationName__, variables: __gl_variables__);
         return __gl_handler__.handle(__gl_payload__)
         .map((e) {
           return ArticleUpdatedResponse.fromJson(e);
         });
   }
}
class _ResolverBase {
   late final GraphLinkCacheStore __gl_store__;
   late final Map<String, _Lock> __gl_tagLocks__;
   final Future<String> Function(String payload) __gl_adapter__;
   _ResolverBase(GraphLinkCacheStore store, Map<String, _Lock> locks, this.__gl_adapter__) {
      __gl_store__ = store;
      __gl_tagLocks__ = locks;
   }
   Future<String> glCallAdapter(GraphLinkPayload payload) async {
      return await __gl_adapter__(json.encode(payload.toJson()));
   }
   Future<_GraphLinkCacheEntry?> _getFromCache(String key, List<String> tags, bool staleIfOffline) async {
      var result = await __gl_store__.get(key);
      if (result != null) {
         var entryMap = jsonDecode(result);
         var entry = _GraphLinkCacheEntry.fromJson(entryMap);
         if (entry.isExpired) {
            if (staleIfOffline) {
               return entry.asStale();
            }
            __gl_store__.invalidate(key);
            if (tags.isNotEmpty) {
               _removeKeyFromTags(key, tags);
            }
            return null;
         } else {
            return entry;
         }
      }
      return null;
   }
   Future<void> _invalidateByTags(List<String> tags) async {
      for (var tag in tags) {
         final tagKey = "${tagKeyPrefix}${tag}";
         final lock = __gl_tagLocks__[tag]!;
         await lock.synchronized(() async
         {
            final data = await __gl_store__.get(tagKey);
            if (data != null) {
               final entry = _GraphLinkTagEntry.decode(data);
               for (var key in entry.keys) {
                  await __gl_store__.invalidate(key);
               }
               await __gl_store__.invalidate(tagKey);
            }
         }
         );
      }
   }
   Future<void> _addKeyToTags(String key, List<String> tags) async {
      for (var tag in tags) {
         final tagKey = "${tagKeyPrefix}${tag}";
         final lock = __gl_tagLocks__[tag]!;
         await lock.synchronized(() async
         {
            final data = await __gl_store__.get(tagKey);
            final entry = data != null ? _GraphLinkTagEntry.decode(data) : _GraphLinkTagEntry({});
            entry.add(key);
            await __gl_store__.set(tagKey, entry.encode());
         }
         );
      }
   }
   Future<void> _removeKeyFromTags(String key, List<String> tags) async {
      for (var tag in tags) {
         final tagKey = "${tagKeyPrefix}${tag}";
         final lock = __gl_tagLocks__.putIfAbsent(tag, () => _Lock());
         await lock.synchronized(() async
         {
            final data = await __gl_store__.get(tagKey);
            if (data != null) {
               final entry = _GraphLinkTagEntry.decode(data);
               entry.remove(key);
               if (entry.keys.isEmpty) {
                  await __gl_store__.invalidate(tagKey);
               } else {
                  await __gl_store__.set(tagKey, entry.encode());
               }
            }
         }
         );
      }
   }
}
class GraphLinkClient {
   final __gl_fragmentMap__ = <String, String>{};
   final __gl_tagLocks__ = <String, _Lock>{};
   late final GraphLinkQueries queries;
   late final GraphLinkMutations mutations;
   late final GraphLinkSubscriptions subscriptions;
   late final GraphLinkCacheStore store;
   GraphLinkClient({
      required Future<String> Function(String payload) adapter,
      required GraphLinkWebSocketAdapter wsAdapter,
      GraphLinkCacheStore? store
   }) {
      __gl_fragmentMap__['_all_fields_Article'] = 'fragment _all_fields_Article on Article{id title authorId author{id name}}';
      __gl_fragmentMap__['_all_fields_Author'] = 'fragment _all_fields_Author on Author{id name articles{..._all_fields_Article}}';
      this.store = store ?? InMemoryGraphLinkCacheStore();
      final tags = [];
      for (var tag in tags) {
         __gl_tagLocks__[tag] = _Lock();
      }
      queries = GraphLinkQueries(adapter, __gl_fragmentMap__, this.store, __gl_tagLocks__);
      mutations = GraphLinkMutations(adapter, this.store, __gl_tagLocks__);
      subscriptions = GraphLinkSubscriptions(adapter, wsAdapter, this.store, __gl_tagLocks__);
   }
   GraphLinkClient.fromUrl({
     required Future<String> Function(String payload) adapter,
     required String wsUrl,
     Future<Map<String, String>?> Function()? wsHeadersProvider,
     GraphLinkCacheStore? store,
   }) : this(
     adapter: adapter,
     wsAdapter: DefaultGraphLinkWebSocketAdapter(url: wsUrl, headersProvider: wsHeadersProvider),
     store: store,
   );
   GraphLinkClient.withHttp({
     required String url,
     required String wsUrl,
     Future<Map<String, String>?> Function()? wsHeadersProvider,
     Future<Map<String, String>?> Function()? headersProvider,
     GraphLinkCacheStore? store,
   }) : this(
     adapter: GraphLinkDioAdapter(url: url, headersProvider: headersProvider).call,
     wsAdapter: DefaultGraphLinkWebSocketAdapter(url: wsUrl, headersProvider: wsHeadersProvider),
     store: store,
   );
}

   enum _HandshakeStatus { none, progress, acknowledged }

   class _SubscriptionHandler {
     static const hexDigits = '0123456789abcdef';
     final _random = Random();
     final Map<String, StreamController<Map<String, dynamic>>> _map = {};
     final Map<String, StreamSubscription> _subs = {};
     final Map<String, GraphLinkPayload> _payloads = {};
     final GraphLinkWebSocketAdapter adapter;

     final pongMessage = jsonEncode(GraphLinkSubscriptionErrorMessage(type: GraphqlWsMessageTypes.pong).toJson());

     _SubscriptionHandler(this.adapter) {
       adapter.onReconnect.listen((_) => _onReconnect());
     }

     Completer<_StreamSink>? _sinkCompleter;
     _HandshakeStatus _handshakeStatus = _HandshakeStatus.none;
     Completer<_StreamSink>? _handshakeCompleter;

     late final _onMessageStream = adapter.onMessageStream;

     Future<_StreamSink> _initWs() {
       if (_sinkCompleter != null) return _sinkCompleter!.future;
       _sinkCompleter = Completer();
       _connect();
       return _sinkCompleter!.future;
     }

     Future<void> _connect() async {
       final completer = _sinkCompleter;
       try {
         await adapter.connect();
         if (_handshakeStatus == _HandshakeStatus.acknowledged) {
           completer?.complete(_StreamSink(sendMessage: adapter.sendMessage, stream: _onMessageStream));
         } else {
           completer?.complete(await _doHandshake());
         }
       } catch (e) {
         completer?.completeError(e);
         _sinkCompleter = null;
       }
     }

     Future<_StreamSink> _doHandshake() {
       if (_handshakeCompleter != null) return _handshakeCompleter!.future;
       _handshakeCompleter = Completer();
       _handshakeStatus = _HandshakeStatus.progress;
       _performHandshake().then((sink) {
         _handshakeCompleter?.complete(sink);
         _handshakeCompleter = null;
       }).catchError((e) {
         _handshakeCompleter?.completeError(e);
         _handshakeCompleter = null;
       });
       return _handshakeCompleter!.future;
     }

     Future<_StreamSink> _performHandshake() async {
       try {
         final payload = await adapter.connectionInitPayload();
         final initMsg = jsonEncode({
           'type': GraphqlWsMessageTypes.connectionInit,
           if (payload != null) 'payload': payload,
         });
         await adapter.sendMessage(initMsg);
         await _onMessageStream
             .map(_parseEvent)
             .firstWhere(
               (msg) => msg.type == GraphqlWsMessageTypes.connectionAck,
               orElse: () => throw StateError('WebSocket closed before receiving connection_ack'),
             )
             .catchError((_) => GraphLinkSubscriptionErrorMessage(type: GraphqlWsMessageTypes.connectionAck));
         _handshakeStatus = _HandshakeStatus.acknowledged;
         return _StreamSink(sendMessage: adapter.sendMessage, stream: _onMessageStream);
       } catch (e) {
         _handshakeStatus = _HandshakeStatus.none;
         rethrow;
       }
     }

     Future<void> _onReconnect() async {
       if (_handshakeStatus == _HandshakeStatus.progress) return;
       _handshakeStatus = _HandshakeStatus.none;
       if (_payloads.isEmpty) return;
       _sinkCompleter = Completer();
       final completer = _sinkCompleter;
       try {
         final sink = await _doHandshake();
         completer?.complete(sink);
         for (final entry in _payloads.entries) {
           final id = entry.key;
           // Listener may have been cancelled when the initial connection failed;
           // re-establish it so incoming events reach the stream controller.
           if (!_subs.containsKey(id)) {
             final sub = sink.stream
                 .map(_parseEvent)
                 .where((event) => event.id == id)
                 .listen((msg) => _handleMessage(msg, id));
             _subs[id] = sub;
           }
           final msg = GraphLinkSubscriptionMessage(
               id: id,
               type: GraphqlWsMessageTypes.subscribe,
               payload: GraphLinkSubscriptionPayload(
                 query: entry.value.query,
                 operationName: entry.value.operationName,
                 variables: entry.value.variables,
               ));
           await sink.sendMessage(json.encode(msg.toJson()));
         }
       } catch (e) {
         completer?.completeError(e);
         _sinkCompleter = null;
         for (final uuid in _map.keys) {
           if (!_map[uuid]!.isClosed) _map[uuid]!.addError(e);
         }
       }
     }

     StreamController<Map<String, dynamic>> _createStremController(String uuid) {
       var controller = StreamController<Map<String, dynamic>>(
         onCancel: () {
           _removeController(uuid);
         },
       );
       _map[uuid] = controller;
       return controller;
     }

     Stream<Map<String, dynamic>> handle(GraphLinkPayload pl) {
       String uuid = _generateUuid();
       var controller = _createStremController(uuid);
       _payloads[uuid] = pl;

       _initWs().then((streamSink) async {
         var sub = streamSink.stream
             .map(_parseEvent)
             .where((event) => event.id == uuid)
             .listen((msg) => _handleMessage(msg, uuid));
         _subs[uuid] = sub;
         var message = GraphLinkSubscriptionMessage(
             id: uuid,
             type: GraphqlWsMessageTypes.subscribe,
             payload: GraphLinkSubscriptionPayload(
               query: pl.query,
               operationName: pl.operationName,
               variables: pl.variables,
             ));
         await streamSink.sendMessage(json.encode(message.toJson()));
       }).catchError((e) {
         if (!controller.isClosed) controller.addError(e);
         _subs.remove(uuid)?.cancel();
         // Keep _payloads[uuid] and _map[uuid] alive — _onReconnect will
         // re-establish the listener and re-send the subscribe message.
       });

       return controller.stream;
     }

     GraphLinkSubscriptionErrorMessageBase _parseEvent(String event) {
       var map = jsonDecode(event);
       var payload = map["payload"];
       if (payload is Map) {
         return GraphLinkSubscriptionMessage.fromJson(map);
       }
       return GraphLinkSubscriptionErrorMessage.fromJson(map);
     }

     void _handleMessage(GraphLinkSubscriptionErrorMessageBase msg, String uuid) {
       var controller = _map[uuid];
       if (controller == null) return;
       switch (msg.type!) {
         case GraphqlWsMessageTypes.ping:
           adapter.sendMessage(pongMessage);
           break;
         case GraphqlWsMessageTypes.next:
           if (!controller.isClosed) controller.add((msg as GraphLinkSubscriptionMessage).payload!.data!);
           break;
         case GraphqlWsMessageTypes.complete:
           _removeController(uuid);
           break;
         case GraphqlWsMessageTypes.error:
           var errorMsg = msg as GraphLinkSubscriptionErrorMessage;
           if (!controller.isClosed) controller.addError(errorMsg.payload as Object);
           _removeController(uuid);
           break;
         default:
       }
     }

     void _removeController(String uuid) {
       _subs.remove(uuid)?.cancel();
       _map.remove(uuid)?.close();
       _payloads.remove(uuid);
       if (_map.isEmpty) {
         _sinkCompleter = null;
       }
     }

     String _generateRandomString(int length) {
       final buffer = StringBuffer();
       for (var i = 0; i < length; i++) {
         final randomIndex = _random.nextInt(hexDigits.length);
         buffer.write(hexDigits[randomIndex]);
       }
       return buffer.toString();
     }

     String _generateUuid([String separator = "-"]) {
       return [
         _generateRandomString(8),
         _generateRandomString(4),
         _generateRandomString(4),
         _generateRandomString(4),
         _generateRandomString(12),
       ].join(separator);
     }
   }


   class GraphqlWsMessageTypes {
     /// Client initializes connection.
     /// Example: { "type": "connection_init", "payload": { "authToken": "abc123" } }
     static const String connectionInit = 'connection_init';

     /// Server acknowledges connection.
     /// Example: { "type": "connection_ack" }
     static const String connectionAck = 'connection_ack';

     /// Client subscribes to an operation.
     /// Example:
     /// {
     ///   "id": "1",
     ///   "type": "subscribe",
     ///   "payload": { "query": "...", "variables": {} }
     /// }
     static const String subscribe = 'subscribe';

     /// Client or server pings for keep-alive.
     /// Example: { "type": "ping", "payload": {} }
     static const String ping = 'ping';

     /// Response to ping.
     /// Example: { "type": "pong" }
     static const String pong = 'pong';

     /// Server sends subscription data.
     /// Example:
     /// {
     ///   "id": "1",
     ///   "type": "next",
     ///   "payload": { "data": { "newMessage": { "id": "42", "content": "Hi" } } }
     /// }
     static const String next = 'next';

     /// Server sends a fatal error for a subscription.
     /// Example: { "id": "1", "type": "error", "payload": { "message": "Validation failed" } }
     static const String error = 'error';

     /// Client or server completes subscription.
     /// Example: { "id": "1", "type": "complete" }
     static const String complete = 'complete';
   }


   class _StreamSink {
     final Future<void> Function(String) sendMessage;
     final Stream<String> stream;

     _StreamSink({required this.sendMessage, required this.stream});
   }

   abstract class GraphLinkWebSocketAdapter {
     Future<void> connect();

     /// Must return a broadcast stream.
     Stream<String> get onMessageStream;

     Future<void> sendMessage(String message);

     Future<void> close();

     // Override to pass auth token or any connection metadata
     Future<Map<String, dynamic>?> connectionInitPayload() async => null;

     // Fires after a successful reconnect; used by the handler to re-subscribe
     Stream<void> get onReconnect => const Stream.empty();
   }

