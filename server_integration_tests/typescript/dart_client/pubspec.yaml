name: graphlink_server_integration_tests_dart_client
description: Dart client integration tests against the generated TypeScript Express+Apollo server.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=2.19.0 <4.0.0"

dependencies:
  graphlink:
    path: ../../../
  http: ^1.2.0
  web_socket_channel: ^3.0.0
  dio: ^4.0.6

dev_dependencies:
  test: ^1.24.0
