server:
  port: 9996

spring:
  graphql:
    websocket:
      path: /graphql
    path: /graphql
    graphiql:
      enabled: true
      path: /graphiql

management:
  server:
    port: 9990
  endpoint:
    shutdown:
      enabled: true
  endpoints:
    web:
      exposure:
        include: shutdown
