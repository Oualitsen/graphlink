server:
  port: 9994

spring:
  graphql:
    websocket:
      path: /graphql
    path: /graphql

management:
  server:
    port: 9992
  endpoint:
    shutdown:
      enabled: true
  endpoints:
    web:
      exposure:
        include: shutdown
