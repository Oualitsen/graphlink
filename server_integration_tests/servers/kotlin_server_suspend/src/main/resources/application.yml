server:
  port: 9995

spring:
  graphql:
    websocket:
      path: /graphql
    path: /graphql

management:
  server:
    port: 9993
  endpoint:
    shutdown:
      enabled: true
  endpoints:
    web:
      exposure:
        include: shutdown
