name: capture_errors_dart
description: GraphLink @glCaptureErrors example — Dart client
version: 1.0.0
publish_to: none

environment:
  sdk: ">=2.19.0 <4.0.0"

dependencies:
  graphlink:
    path: ../../../
  dio: ^5.9.2
