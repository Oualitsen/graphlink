name: dart_http_upload_app
description: GraphLink upload example — Dart client with package:http adapter
version: 1.0.0
publish_to: none

environment:
  sdk: ">=2.19.0 <4.0.0"

dependencies:
  http: ^1.2.0
  web_socket_channel: ^3.0.3
